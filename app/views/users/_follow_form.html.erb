<% unless current_user?(@user) %>
  <% if current_user.following?(@user) %>
    <div id="follow_form">
      <%= render 'unfollow' %>
    </div>
  <% elsif !@user.is_deleted %>
    <div id="follow_form">
      <%= render 'follow' %>
    </div>
  <% end %>
<% end %>
